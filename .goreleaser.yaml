version: 2
project_name: aerovoice

before:
  hooks:
    - go mod tidy

builds:
  - id: vcs
    binary: vcs
    main: ./cmd/vcs
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X github.com/sh0jitmy/aerovoice/internal/version.Version={{.Version}}
      - -X github.com/sh0jitmy/aerovoice/internal/version.Commit={{.Commit}}
      - -X github.com/sh0jitmy/aerovoice/internal/version.Date={{.Date}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

  - id: grs-emulator
    binary: grs-emulator
    main: ./cmd/grs-emulator
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X github.com/sh0jitmy/aerovoice/internal/version.Version={{.Version}}
      - -X github.com/sh0jitmy/aerovoice/internal/version.Commit={{.Commit}}
      - -X github.com/sh0jitmy/aerovoice/internal/version.Date={{.Date}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - formats:
      - tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs?:'
      - '^test:'
